"use client";

export function OrderHistory() {
  return (
    <div className="rounded-xl border border-dashed border-kape-300 bg-white p-6 text-sm text-kape-700">
      Order history is unavailable because ordering tables were removed.
    </div>
  );
}
